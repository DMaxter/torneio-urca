apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - http-route.yaml

patches:
  - target:
      kind: Deployment
      name: urca-tournament
    path: deployment-patch.yaml

images:
  - name: ghcr.io/dmaxter/urca-tournament
    newTag: latest
